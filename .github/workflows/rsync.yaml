name: Docker Image CI - rsync

on:
  push:
    branches: [ master ]
    tags: ['v*']
    paths: [ "rsync/**", ".github/workflows/rsync.yaml", ".github/workflows/build-and-push.yaml" ]
  pull_request:
    branches: [ master ]
    paths: [ "rsync" ]
  workflow_dispatch:

jobs:

  build-and-push:
    uses: ./.github/workflows/build-and-push.yaml
    with:
      app: rsync

name: Docker Image CI - Samba

on:
  push:
    branches: [ master ]
    paths: [ "samba", ".github/workflows/samba.yaml" ]
  pull_request:
    branches: [ master ]
    paths: [ "samba" ]
  workflow_dispatch:

jobs:

  build-and-push:
    uses: ./.github/workflows/build-and-push.yaml
    with:
      app: samba

name: Docker Image CI - speedtest-go

on:
  push:
    branches: [ master ]
    paths: [ "speedtest-go", ".github/workflows/speedtest-go.yaml", ".github/workflows/build-and-push.yaml" ]
  pull_request:
    branches: [ master ]
    paths: [ "speedtest-go" ]
  tags:
    - 'v*'
  workflow_dispatch:

jobs:

  build-and-push:
    uses: ./.github/workflows/build-and-push.yaml
    with:
      app: speedtest-go

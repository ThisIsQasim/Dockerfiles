name: Docker Image CI - apcupsd

on:
  push:
    branches: [ master ]
    paths: [ "apcupsd", ".github/workflows/apcupsd.yaml" ]
  pull_request:
    branches: [ master ]
    paths: [ "apcupsd" ]
  workflow_dispatch:

jobs:

  build-and-push:
    uses: ./.github/workflows/build-and-push.yaml
    with:
      app: apcupsd
